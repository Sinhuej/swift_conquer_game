import 'package:flame/game.dart';

class SwiftConquerFlameGame extends FlameGame {
  @override
  Future<void> onLoad() async {
    // Phase 33–40: Flame shell only
  }

  @override
  void update(double dt) {
    super.update(dt);
  }

  @override
  void render(Canvas canvas) {
    super.render(canvas);
  }
}
