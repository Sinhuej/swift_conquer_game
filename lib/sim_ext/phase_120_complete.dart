/// Phase 120 complete.
/// AI depth foundation (policies, personality, difficulty, deterministic RNG,
/// cadence runner, and tests) is present and stable (headless).
const bool phase120Complete = true;
