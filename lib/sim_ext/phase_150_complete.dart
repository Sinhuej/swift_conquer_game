/// Phase 150 complete.
/// AI wiring layer (engine-agnostic command envelopes), agent registry,
/// scenario/content catalogs, balance presets, and validation tests exist
/// and are CI-stable (headless).
const bool phase150Complete = true;
